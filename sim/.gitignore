# Build outputs
meshsim
meshnode
meshnode-*
*.dSYM/
*.o

# Runtime state (created when the sims run)
child_cfg.bin
fsroot/
vendor/

# Scratch screenshot output (SIM_SCRIPT mode); curated copies live in docs/screenshots/
shots/
